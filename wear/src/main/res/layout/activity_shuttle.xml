<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/constraintShuttleLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/android_like_blue">

    <androidx.wear.widget.WearableRecyclerView
        android:id="@+id/wear_shuttle_recycler_view"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scrollbars="vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ProgressBar
        android:id="@+id/indeterminateBar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="@+id/constraintShuttleLayout"
        app:layout_constraintEnd_toEndOf="@+id/constraintShuttleLayout"
        app:layout_constraintStart_toStartOf="@+id/constraintShuttleLayout"
        app:layout_constraintTop_toTopOf="@+id/constraintShuttleLayout" />

</androidx.constraintlayout.widget.ConstraintLayout>